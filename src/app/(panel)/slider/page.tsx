import SliderClient from "@/components/SliderClient";

export const dynamic = "force-dynamic";

/** Ana sayfa slider küratörlüğü — hangi haberler hangi sırada (EDITOR/ADMIN). */
export default function SliderPage() {
  return <SliderClient />;
}
