import ContributionsClient from "@/components/ContributionsClient";

export const dynamic = "force-dynamic";

/** Katkı Kuyruğu — kullanıcı hata bildirimleri/önerileri. EDITOR + ADMIN. */
export default function ContributionsPage() {
  return <ContributionsClient />;
}
