import ReportersClient from "@/components/ReportersClient";

export const dynamic = "force-dynamic";

/** Muhabir Başvuruları — Saha Muhabiri programı yönetimi. EDITOR + ADMIN. */
export default function ReportersPage() {
  return <ReportersClient />;
}
